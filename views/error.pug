extends layout.pug 

block content 
    //Handles errors
    h1 Error 
    p= `An error occured: ${error.message}`
    
    script.
        console.error("An error occurred: #{error.message}");
    
    // Route to front page
    nav.nav
      a(href='/')
        span &larr;
        |  Back